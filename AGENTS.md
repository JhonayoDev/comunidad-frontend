# AGENTS.md — Comunidad Frontend

## Goal
Alinear todos los módulos del frontend (GUARDIA y RESIDENTE) con los endpoints reales del backend, manteniendo responsividad mobile-first con PrimeVue + Tailwind.

## Constraints & Preferences
- No modificar el backend
- Usar PrimeVue 4.5.5 (Card, Avatar, Tag, Badge, Button, Dialog, Select, Textarea, InputText, Message, Skeleton, Divider, Password)
- Mobile-first responsive con CSS/Tailwind + PrimeVue
- pnpm, nunca npm
- Sin datos mock — todos los servicios llaman directamente al API
- `console.error` en todos los catch blocks para depuración visible
- Todos los endpoints scoped por condominio: `/api/v1/condominios/{condominioId}/...`

## Done
- **Modelo de planilla persona-por-fila (manual + wizard + importación + futuro CRM)**: la planilla de datos del condominio pasó a un **formato persona-por-fila** (una fila = una persona vinculada a una casa, con vehículos/estacionamientos en columnas fijas `patente1..3`/`est1..3` y bodegas `bodega1..3` si `capacidadBodegas>0`), reemplazando el viejo formato de filas repetidas por vehículo. **`src/data/planillaColumnas.js`** es la fuente única del esquema (`COLUMNAS_BASE`, `columnasVehiculos`, `columnasBodegas`, `generarColumnas(capacidad)`, `filaAPayload` → payload anidado `{unidad, tipoUnidad, sector, persona, vinculo, vehiculos[], bodegas[]}`). **`src/composables/usePlanillaDatos.js`**: estado de filas, `validarFila` (mismas reglas del import + dedupe contra backend: unidades/emails/patentes existentes + patentes duplicadas en archivo), `marcarResponsable` (1 `es_responsable` por casa), **borrador en sessionStorage** (autoguardado con `watch` deep, `cargarBorrador`/`descartarBorrador`, sobrevive recargas y pérdida de señal, se pierde al cerrar pestaña), `buildPayload` (todas las filas en una sola petición) y `enviar` simulado. **`src/components/planilla/PlanillaDatos.vue`**: componente reutilizable — agrupa por casa (colapsable, responsable primero), filtro por casa, botón "+" para nueva fila, celdas editables (InputText/Select/Checkbox), errores por fila, mobile cards + desktop fila tipo Excel. **`ImportacionMasivaView.vue`** refactorizada para usar el componente (mismo formato CSV persona-por-fila, ejemplo demo y plantilla actualizados). **`SetupPlanillaView.vue`**: paso 1 del wizard = planilla completa (reemplaza y elimina `SetupUnidadesView`). **`useSetupConfiguracion.js`**: 5 pasos (`planilla` real + `accesos`/`areas-comunes`/`cargos`/`personal` como placeholders `EnConstruccionView` en el router); `pasoCompletado("planilla")` = `residentesActivos > 0`. Solicitud backend actualizada a v2.0: mismo contrato para archivo y JSON, `es_responsable` (nuevo campo en `VinculoPersonaUnidad`, máx 1 por unidad), batch JSON en preview/ejecutar. Build OK + tests 103/103 (`usePlanillaDatos.test.js` nuevo, `csvParser.test.js` y `useSetupConfiguracion.test.js` actualizados)
- **Simulación de importación masiva Excel/CSV + solicitud backend**: diseñada la carga de datos completa del condominio desde `.csv`/`.xlsx`. Solicitud `docs/solicitudes-backend/SOLICITUD_IMPORTACION_MASIVA_EXCEL_CSV.md` — 2 fases: `POST /importaciones/preview` (valida fila a fila sin persistir, devuelve `importacionId` + estados OK/ERROR) y `POST /importaciones/{id}/ejecutar` (aplica en una transacción por orden FK: sectores→unidades→personas→vínculos→vehículos→estacionamientos, con dedupe por email/numero/patente y reglas reales verificadas: `uq_unidad_numero`, `uq_vehiculo_cond_patente`, email/rut global UNIQUE, máx 1 PROPIETARIO/ARRENDATARIO activo por unidad, `plan.unidadLimit` + `capacidadPara`). Formato: fila = persona vinculada a unidad con 0..N vehículos (mismo email = filas repetidas); permiso nuevo `IMPORTACION_DATOS`. **Frontend**: `src/utils/csvParser.js` (parser CSV puro, delimitador `;`/`,` autodetectado, comillas, BOM, CRLF, multilínea) + `ImportacionMasivaView.vue` (ruta `importacion`, nav admin): carga archivo real o ejemplo demo, valida localmente fila a fila (mismas reglas del contrato), tabla planilla desktop + cards mobile, botón "Importar datos" simulado (endpoint pendiente backend) y descarga de plantilla. Build OK + tests 92/92 (`csvParser.test.js` nuevo)
- **Wizard de configuración del administrador (`SetupLayout`/`SetupPlanillaView` + banner en `AdminDashboardView`)**: nuevo flujo guiado post-activación, DISTINTO del `SaasCondominioSetupView` (ese lo vive el SUPER_ADMIN antes de que el admin entre; este lo vive el ADMINISTRADOR dentro de la app). Cada paso es una **vista completa** (sub-rutas `/setup/*`, no desplegables), con barra de progreso y navegación. Ruta base `setup` (meta `ADMINISTRADOR`/cargos) con `redirect` al primer paso pendiente. **Paso 1: Planilla** (`SetupPlanillaView.vue`) — la planilla persona-por-fila completa vía `PlanillaDatos.vue` (reemplaza el viejo `SetupUnidadesView`); `emit("actualizado")` refresca el progreso del layout vía `router-view` slot. **Progreso derivado de datos** (sin tocar backend): `useSetupConfiguracion.js` expone `SETUP_PASOS` (5 pasos: `planilla` real + `accesos`/`areas-comunes`/`cargos`/`personal` como placeholders), `pasos`, `primerPasoPendiente`, `configuraciónCompleta`, `progreso`, `cargar()` (consume `dashboardService.admin`) y `sincronizarTotales()` (para el banner sin fetch extra). Banner "Configuración pendiente" (Card con "Falta: ..." + botón "Continuar configuración") en `AdminDashboardView` derivado de `dashboard.totales`. Ítems nav "Configurar" agregados en `NAV_ITEMS_BY_ROLE.ADMINISTRADOR` y `CARGO_NAV_ITEMS.ADMINISTRADOR`. **Fix bug de sectores**: `UnidadesView` hacía `Promise.all([getUnidades, getSectores])` y `GET /condominios/{cid}/sectores` NO existe en backend → 404 rompía la vista; ahora `getSectores` degrada suave (catch → `[]`) y `sectorLabel`/`optionLabel` usan `nombre` (campo real de la entidad `Sector`). Build OK + tests 103/103 (`useSetupConfiguracion.test.js` actualizado)
- **Permisos por cargo auditados contra el backend + `MisPermisosView` alineada al catálogo real**: verificado que el backend resuelve permisos efectivos = rol (`rol_permisos`) + cargo (`cargo_permisos`) en `PermisosCondominioService.resolverPermisos` vía `GET /me/permisos`, pero **NO existen endpoints de administración de permisos** (ni catálogo, ni lectura/edición por cargo). Creado `src/data/permisosCatalogo.js` con el catálogo REAL del backend (códigos de las migraciones V2→V39: `ACCESO_REGISTRAR_INGRESO/SALIDA`, `ENCOMIENDA_CREAR/ENTREGAR`, `FINANZA_VER/GESTIONAR`, `GASTO_CREAR/ANULAR`, `PAGO_RESIDENTE_*`, `CASO_*`, `DASHBOARD_TESORERO`, permisos SaaS V25, etc. — **sin códigos inventados como `ACCESO_REGISTRAR`, `ENCOMIENDA_REGISTRAR` o `FINANZAS_VER` del viejo mock**). `MisPermisosView.vue` (todos los roles) ahora cruza `auth.permisos` (reales de `/me/permisos`) contra el catálogo real con `title` descripcion, y muestra una sección "Permisos sin catalogar" (warn) para códigos reales aún no documentados (evita ocultarlos). Las vistas `PermisosMatrixView` y `CargosPermisosView` (100% mock + guardado simulado, códigos inexistentes en backend) pasaron a `EnConstruccionView` vía router (precedente `SolicitudesView`) — los archivos `.vue` se conservan para reconectarlos. Creada `docs/solicitudes-backend/SOLICITUD_ADMIN_PERMISOS_POR_CARGO.md` (catálogo `GET /admin/permisos/catalogo`, permisos por cargo `GET/PUT /admin/cargos/{cargo}/permisos`, permiso `ROL_GESTIONAR`, reemplazo total + auditoría `cargoPermisoEditado`). Build OK + tests 82/82
- **`prime-theme.js`**: agregado override `multiselect` (overlay, option, chip, emptyMessage) siguiendo el patrón de `select`/`autocomplete` (`color-mix(primary.surface 98%)` + `textPrincipal`) — el dropdown de `MultiSelect` en canales se veía en blanco en dark (usaba tokens globales `overlay.select.*` no personalizados). Ahora responde a claro/oscuro
- **Panel SaaS del catálogo global de reglas de notificación (`SaasReglasNotificacionView.vue`)**: nueva vista en `superadmin/reglas-notificacion` (roles SUPER_ADMIN/SOPORTE) que consume el refactor `refactor/reglas-notificacion-catalogo-bd` del backend (catálogo global en BD, migraciones V54-V56). Usa `GET /admin/notificaciones/catalogo` (permiso `REGLA_NOTIF_VER`) y `PUT /admin/notificaciones/catalogo/{tipo}` (permiso `REGLA_NOTIF_GESTIONAR`, auditoría `reglaNotificacionEditada`). Cards con audiencia (enum `TipoAudiencia`), prioridad, canales (array JSON, `MultiSelect`), obligatoriedad POR CANAL (`esObligatoriaInapp/Email/Push`) y `visibleUsuario`; edición completa en `Dialog` (PUT exige todos los campos obligatorios) con `ConfirmDialog` al guardar y aviso de descarte con cambios. Agregado `adminService.listarCatalogoReglas/obtenerReglaCatalogo/actualizarReglaCatalogo` y nav "Reglas Notif. Global" para SUPER_ADMIN/SOPORTE. **El ítem viejo "Reglas Notif." (vista por-condominio) se quitó del menú SUPER_ADMIN** (ese rol solo ve la global; la ruta `notificaciones/reglas` quedó restringida a ADMINISTRADOR). Build OK + tests 82/82
- **`ReglasNotificacionView.vue` alineada al refactor de catálogo (backend V54-V56)**: la vista por-condominio ahora consume el nuevo contrato — `canales` como **array JSON** (`MultiSelect`, no `Select multiple`), obligatoriedad **POR CANAL** (`esObligatoriaInapp/Email/Push`) y `visibleUsuario`. `reglasCatalogo.js` actualizado al seed V54 real (`esObligatoria` booleano → 3 booleans por canal + `visibleUsuario`). `useReglasNotificacion.js` mergea override+catálogo con los campos por canal y **envía el estado completo combinado en cada PUT** (el backend hace upsert pisando todos los campos: null = heredar del global — enviar solo `{canales}` re-habilitaba reglas deshabilitadas). `canalObligatorio(r, canal)` deriva el flag por canal para el Tag "· obligatorio"
- **Planes inactivos visibles + reactivación (backend implementado)**: `GET /admin/planes?incluirInactivos=true` (default `false`) y `PATCH /admin/planes/{id}/reactivar` (permiso `PLAN_GESTIONAR`, auditoría `PLAN_REACTIVAR`). Frontend: `adminService.listarPlanes(incluirInactivos = false)` + `reactivarPlan(id)`; `SaasPlanesView` carga con `listarPlanes(true)` (los inactivos persisten tras recargar), botón "Reactivar" (con `ConfirmDialog`) en cards inactivas, y desactivar/reactivar actualizan el estado local sin refetch. Además: confirmaciones `ConfirmDialog` para desactivar y crear plan (resumen del plan), y el error 409 del backend ("Ya existe un plan con el código 'X'") se muestra en un `Message` dentro del diálogo. Solicitud `SOLICITUD_PLANES_INACTIVOS_REACTIVAR.md` implementada. Build OK + tests 82/82
- **`UnidadesPersonasView` pasó a ser vista exclusiva del ADMINISTRADOR del condominio**: quitado del menú del SUPER_ADMIN (nav + ruta `notificaciones/unidades-personas`, ahora `meta: { roles: ["ADMINISTRADOR"], cargos: ["ADMINISTRADOR"] }` sin `SUPER_ADMIN`) y agregado al menú del ADMINISTRADOR (rol `NAV_ITEMS_BY_ROLE.ADMINISTRADOR` + cargo `CARGO_NAV_ITEMS.ADMINISTRADOR`). Eliminadas las entradas muertas de PRESIDENTE/SECRETARIO (la ruta nunca les dio acceso). El guard `beforeEach` ya redirige a un SUPER_ADMIN que intente entrar por URL. Build OK + tests 82/82
- **Capacidad de unidades por tipo (contrato SUPER_ADMIN)**: el SUPER_ADMIN declara el techo de unidades por tipo del condominio y el ADMINISTRADOR solo puede crear dentro de ese límite. **Backend (V51, implementado)**: columnas `capacidad_casas/departamentos/estacionamientos/bodegas/otro` en `condominios` (NULL = sin tope), `Condominio.capacidadPara(TipoUnidad)`, `UnidadRepository.countByCondominioIdAndTipoAndActivoTrue`, `UnidadService.validarCapacidadTipo` en `crear` y en `actualizar` solo si cambia el tipo, `ActualizarCondominioRequest`/`CondominioDetalleAdminResponse` + capacidades y conteos por tipo, `SaasAdminService.actualizar` valida **suma ≤ plan.unidadLimit** (estacionamientos/bodegas/OTRO consumen cupo del plan). **Frontend**: wizard sección 2 → "Capacidad de unidades" (5 InputNumber precargados + uso actual + total declarado + límite del plan desde `listarPlanes`; "Guardar sección" → PATCH + `CREAR_UNIDADES`); `SaasCondominioDetailView` card "Capacidad de unidades" con "X creadas de Y" y edición inline (PATCH); `UnidadesView` (admin) muestra cupo por tipo, bloquea crear/editar al llegar al tope y **corrige opciones inválidas LOCAL/OFICINA** (no existen en el enum backend → 400) dejando solo CASA/DEPARTAMENTO/ESTACIONAMIENTO/BODEGA/OTRO; ruta `Unidades` conectada a la nav de ADMINISTRADOR (rol + cargo) y cargos PRESIDENTE/SECRETARIO. Solicitudes: `SOLICITUD_CAPACIDAD_UNIDADES_POR_TIPO.md` (implementada, V51) y `SOLICITUD_ENDPOINT_CAPACIDAD_UNIDADES.md` (pendiente — `GET /condominios/{cid}/capacidad-unidades`, permiso `UNIDAD_VER`, para que el admin vea el cupo; el frontend ya lo consume con **degradación suave**: si 404, muestra solo el uso y el backend igual refuerza el límite). Build OK + tests 81/81
- **Wizard "Puesta en marcha" (`SaasCondominioSetupView.vue`)**: nueva ruta `superadmin/condominios/:id/setup` (SUPER_ADMIN) que guía la configuración inicial post-creación. **El proceso no avanza hasta hacer clic en "Guardar"**: las tareas de onboarding (`POST /admin/condominios/{id}/onboarding/tareas/{codigo}/completar`) se completan SOLO con los botones "Guardar almacenamiento"/"Guardar sección", que están **deshabilitados hasta que la sección tenga los datos completos** (`storageValido` = campos del proveedor R2/Drive completos; "Guardar sección" de unidades habilitado con ≥1 unidad creada; de admin habilitado con cargo ADMINISTRADOR/PRESIDENTE activo). 3 secciones con barra de progreso: (1) **Almacenamiento** — form R2/Drive → `PUT configuracion-almacenamiento` → `CONFIGURAR_STORAGE`; (2) **Capacidad de unidades** — declara el techo por tipo (CASA/DEPARTAMENTO/ESTACIONAMIENTO/BODEGA/OTRO) → `PATCH /admin/condominios/{id}` con `capacidad*` → `CREAR_UNIDADES` (el admin crea las unidades dentro del límite); (3) **Administrador/Presidente** — creación completa desde cero: `POST /personas` (409 → `buscarPorEmail` y reusa) → cuenta de usuario (`POST /personas/{id}/usuario`, envía email setup-password automáticamente — verificado en `UsuarioGestionService.crearCuenta`) → cargo (`POST /miembros`, crea el `usuario_condominios` y da acceso al condominio) → `ASIGNAR_ADMIN`. El tipo de acceso se elige con Select "Tipo de acceso": **Administrador externo** = rol `ADMINISTRADOR` + cargo `ADMINISTRADOR`; **Administrador interno** = rol `RESIDENTE` + cargo `ADMINISTRADOR`; **Presidente/Tesorero/Secretario** = rol `RESIDENTE` + cargo respectivo. El form se **prellena con `responsableNombre/Email/Telefono`** del condominio (los datos declarados al crearlo). Nota backend: `crearCuenta` NO crea la fila `usuario_condominios` — el acceso al condominio sale de cargo/vínculo/usuario_condominios (`CondominioAccessService.tieneAcceso`), por eso el externo también recibe cargo; `CONFIGURAR_PLANTILLAS` no tiene lógica real en el wizard — el frontend la **auto-completa al cargar** el wizard (`completarTarea`) para que el progreso refleje las 3 secciones reales y el condominio pueda pasar a `COMPLETADO`. **Paso 4 (final): "Activación de la cuenta"** — el frontend consulta `passwordSetAt` (existe desde `SOLICITUD_DETECCION_CUENTA_ACTIVADA.md`, **implementada V52**) vía `adminService.listarUsuarios(cid, { activo: true, size: 200 })` cruzando `personaId` del miembro activo con cargo ADMINISTRADOR/PRESIDENTE; muestra "Pendiente de activar" mientras el admin creado no configure su contraseña y "Cuenta activada el {fecha}" cuando sí; poll cada 20s mientras esté pendiente + botón "Verificar de nuevo"; `progreso` = 4 pasos (storage, unidades, admin, activación) y el mensaje "Puesta en marcha completa" exige la activación. Usa `useValidacionChile` en el form del admin. El detalle del condominio ahora enlaza al wizard ("Puesta en marcha") en vez de la vista Onboarding. Build OK + tests 81/81
- **Vista de listado de condominios SaaS (`SaasCondominiosView.vue`)**: nueva ruta `superadmin/condominios` (SUPER_ADMIN) que usa el endpoint real `GET /api/v1/admin/condominios` con **paginación y filtros server-side** (`nombre`, `statusPago`, `planId`, `onboardingStatus`, `page`, `size` → `PageResponse<CondominioAdminResumen>`). Filtros en Card (buscar por nombre/responsable con Enter, Select de estado de pago, onboarding y plan desde `listarPlanes`), lista de cards clickeables → `SaasCondominioDetail`, botón "Entrar" → `seleccionarCondominio` + Dashboard, botón "Nuevo" → `SaasCrearCondominio`, `Paginator` para paginación. La nav "Condominios" ahora apunta a `SaasCondominios` y el "Volver" del detalle vuelve a la lista. Build OK + tests 81/81
- **Contexto dual residente/cargo — cargo ADMINISTRADOR habilitado (Fase 1)**: el modelo distingue rol ADMINISTRADOR (externo, sin cargo) de cargo ADMINISTRADOR (residente que ocupa el cargo, revocable). El backend ya soporta ambos (CargoCondominio incluye ADMINISTRADOR/GUARDIA/CONSERJE; permisos efectivos = rol + cargo en el JWT). El frontend ahora lo refleja: `authStore.js` `CARGO_LABELS` += `ADMINISTRADOR` y `CONTEXT_DASHBOARDS` += `administrador: "Dashboard"`; `useNavigation.js` `CARGO_NAV_ITEMS` += bloque `ADMINISTRADOR`; `router/index.js` agregó `"ADMINISTRADOR"` a `meta.cargos` de 20 rutas admin (Dashboard, Vehiculos, SolicitudesAdmin, Residentes, Unidades, Personal, PlantillasNotificacion, Anuncios, ReglasNotificacion, UnidadesPersonas, ConfiguracionAlmacenamiento, Encomiendas, Porton, Visitas, BusquedaDetalle, RegistrarVisita, Bitacora, ChecklistTemplates, Autorizaciones, Archivos); `AppHeader.vue` `goHome` navega por `contextDashboard` (contexto activo) y las píldoras pasaron a tarjetas con slot de badge por contexto (conteo llega en Fase 2). Documentos de solicitud: `SOLICITUD_NOTIFICACIONES_POR_CONTEXTO.md` (columna `audiencia` en notificaciones + `noLeidasPorAudiencia` en snapshot SSE + `GET /me/cargos` + push con prefijo) y `SOLICITUD_CARGO_ADMINISTRADOR_CIERRE_ENCOMIENDAS.md` (`CARGOS_PUEDE_CERRAR` no incluye cargo ADMINISTRADOR → residente con ese cargo no puede cerrar). Build OK + tests 52/52
- **Diagnóstico de pantalla blanca PWA iOS (iPhone 12 / iOS 26, sin Mac)**: instrumentado el frontend para capturar el error real de forma remota. `src/utils/frontendErrorReporter.js`: hooks globales (`window.onerror` en fase **captura** — los errores de recurso/chunk 404 no burbujean, `unhandledrejection`, `app.config.errorHandler` vía `reportarError`), overlay DOM independiente de Vue ("Copiar error"/"Cerrar"), persistencia en localStorage (`comunidad:frontend-errors` máx. 5, `comunidad:frontend-hitos` máx. 20, `comunidad:html-loaded` en `index.html`), beacons `sendBeacon` a `/__frontend-error` y `/__frontend-boot` (404s que quedan en los logs de Coolify/Nginx/Cloudflare; **solo en build de producción — en dev se omiten** con `if (import.meta.env.DEV) return` en `beacon()`, para no generar ruido 404 en el dev server), auto-install en el cuerpo del módulo (primer import de `main.js` → atrapa también errores de evaluación/import de vue/router/etc.), hitos `boot:reporter`/`boot:main`/`boot:mounted`, y el reporter nunca crashea (try/catch en todo). Además `src/utils/fechas.js` (`formatearHora`/`formatearFecha` devuelven `""` ante fecha inválida, nunca lanzan `RangeError`) aplicado en `TurnoCard.vue`, `useTurno.js` y `GuardiaDashboardView.vue` (3 sitios con `new Date(...).toLocaleTimeString` sin guard). Tests nuevos (`frontendErrorReporter.test.js`, `fechas.test.js`) — 52/52 OK, build OK
- **Motor de Notificaciones V2 — frontend conectado al backend implementado**: verificado en vivo contra el backend (rama `feature/sseresidente`, commits `d15b3d7`+`0d22b7d`, suite 287 tests). `notificacionesStreamService.js` (cliente SSE de `GET /notificaciones/stream`, evento `notificacion`, permiso `NOTIFICACION_VER`, snapshot `{noLeidas}` + señal `NOTIFICACION_CREADA`) + `useNotificacionesTiempoReal.js` (seed del badge desde snapshot, descarte ante cambio, invalida `["notificaciones-sync", cid]`, gracia 1 min / fallback 2 min) + test. `MainLayout.vue` conecta el stream según permiso (bus independiente de dashboard/residente). `useNotificaciones` pasó de `refetchInterval: 120000` fijo a reactivo (vivo sin polling). `ReglasNotificacionView.vue` ahora edita `audiencia` (incluye `UNIDAD_OCUPANTES`/`UNIDAD_TITULAR`) y `prioridad`. Vista CRM nueva `UnidadesPersonasView.vue` (ruta `notificaciones/unidades-personas`, roles admin/super_admin): lista vínculos por unidad y toggle `recibeNotificaciones` → `PATCH .../vincular-persona-unidad/{id}/recibe-notificaciones` (agregado a `personasService`). `PerfilView.vue`: añadido toggle Push (hoy App/Email/Push). `reglasCatalogo.js`: `ENCOMIENDA_RECIBIDA → UNIDAD_OCUPANTES` (coincide con backend). Build OK + tests 27/27
- **Estilos de dashboard residente alineados con las vistas del guardia**: `ResidenteDashboardView.vue` (MiUnidad) usa ahora el mismo sistema de diseño — filas `p-2 border-round` dentro de `flex flex-col gap-2` (sin `border-t`/`py-2`), íconos de título de Card con `--p-primary-400`, badges con `severity="warn"`, cards sin `mb-4` (el contenedor `gap-4` aporta el espaciado; el `v-for` de unidades agrega `flex flex-col gap-4`) y skeletons que replican la estructura real (4 cards). Mismo tratamiento aplicado a las filas de autorizaciones en `InicioView.vue` y a `TarjetaEncomiendasResidente.vue` (filas clickables `p-2 border-round hover:bg-emphasis`). Build OK + tests 24/24
- **`ChecklistDialog.vue`** (checklist de inicio/fin turno): los `ToggleButton` Sí/No quedaban ilegibles — texto `surface.500` (#8a8a8a) sobre el rojo `#c53b3b` del "No" (light) y píldora blanca en dark (`surface.950` invertido). Añadido override `togglebutton` en `prime-theme.js` (bg `color-mix(surface 75%)`, color `textPrincipal`, checked `{primary.color}` + `#ffffff`) y `color:#fff !important` en los estados `.checklist-true`/`.checklist-false`
- **`FiltrosBitacora.vue`**: creado — componente reutilizable de filtros colapsables (tipo, clasificación, rango de fechas) con `defineModel`; replicado el patrón lupa de Encomiendas en `BitacoraView` (la lupa ahora queda a la derecha del botón "Registrar novedad"; ocultarla resetea los filtros)
- **`prime-theme.js`**: corregidos los diálogos ilegibles (ConfirmDialog de inicio/fin turno y colación, y todos los `<Dialog>`) — `overlay.modal.background` resolvía a `var(--p-surface-0)` = `var(--p-primary-text-principal)` (en light `#1a1a1a`) con texto `--p-text-color` (`#404040`), texto oscuro sobre fondo oscuro. Añadidos overrides `dialog.root` y `confirmdialog.root` con el mismo patrón de popover/select (`color-mix(in srgb, {primary.surface} 98%, transparent)` + `{primary.textPrincipal}`). Verificado con `toVariables()` en light y dark; tests 21/21 y build OK
- Revisados todos los controllers del backend y sus DTOs
- Mapeados todos los campos de request/response contra cada vista del frontend
- Eliminados todos los datos mock de todos los servicios
- **`encomiendasService.js`**: `getMisEncomiendas` usa URL correcta `/condominios/{cid}/mis-encomiendas`
- **`perfilService.js`**: agregado `actualizarMe(data)` → `PUT /me`
- **`useNotificaciones.js`**: corregido — usa `getTodas(condominioId)`, `getSync(condominioId)` (con `refetchInterval: 120000`), `marcarLeida(condominioId, notif.id)`, `marcarTodasLeidas(condominioId)`, obtiene `condominioId` del authStore
- **`RegistrarVisitaView`**: `tipo` usa enum API (`VISITA`, `DELIVERY`, `UBER`, `SERVICIO`, `TECNICO`, `OTRO`); campo `nombreVisitante`; single `unidadId`; eliminado `buscarFrecuente`
- **`EncomiendasView`**: formulario usa `tipo` (CARTA/ENCOMIENDA) + `nombreDestinatario`; entregar pide `nombreRetira` + `rutRetira`;
- **`useEncomiendas`**: `entregar(encomienda, nombreRetira, rutRetira)` envía body al backend
- **`useVisitas`**: `registrarSalida` setea `visita.estado = "FINALIZADO"` y `visita.fechaSalida`
- **`PortonView`**: migrado a usar `<BuscadorPatenteCard />` con `GET /busqueda/por-patente`
- **`RegistrarVisitaView`**: migrado a `useUnidades` con TanStack Query; cambiado Select por AutoComplete para búsqueda por número de casa; acepta pre-fill desde BuscadorPatenteCard
- **`useBusquedaPatente.js`**: creado — orquesta búsqueda por patente + detección de acceso activo para salida rápida; botón "Registrar ingreso" navega a RegistrarVisitaView con datos pre-rellenados
- **`BuscadorPatenteCard.vue`** (`src/components/visitas/`): creado — componente reutilizable con props `compact` para dashboard/vista completa; función `extraerUnidadDeResultado` parsea subtitulo/detalle para pre-fill de casa
- **`ConfirmarSalidaDialog.vue`** (`src/components/visitas/`): creado — subcomponente presentacional para salida rápida con datos del acceso activo
- **`VisitasView`**: template usa `nombreVisitante`, `patenteVisitante`, `tipo`, `fechaIngreso`, `fechaSalida`, `estado`; migrado a PrimeVue
- **`GuardiaDashboardView`**: movimientos usan `nombre`, `unidad`, `fecha`; agregados `console.error` en catch blocks
- **`BitacoraView`**: `registradoPorNombre` (no `realizadoPorNombre`), `registradoEn` (no `realizadoEn`); agregados `console.error`
- **`AutorizacionesView`**: `tipoLabels` coincide con enum API; reemplazado `creadoPorNombre` por `createdAt/fechaInicio`; agregado `console.error`
- **`InicioView (residente)`**: encomiendas usa `tipo`, `nombreDestinatario`, `creadoEn`; agregado `console.error`
- **`NotificacionesView`**: migrado a PrimeVue; usa `notif.id` (no `notificacionId`); pasa `condominioId` en llamadas API
- **`MisEncomiendasView`**: migrado a PrimeVue; usa `tipo`, `nombreDestinatario`, `unidadNumero`, `creadoEn`; pasa `condominioId`
- **`PerfilView`**: migrado a PrimeVue + `<Password>`; usa `perfilService.actualizarMe({nombre, telefono})` real; carga perfil de `GET /me`
- **`TarjetaEncomiendasPendientes.vue`**: creado — card reutilizable para conteo de encomiendas pendientes; en F1 usaba `GET /encomiendas/activas`, hoy conteo en vivo solo vía SSE (`metricas.encomiendasPendientes`) con seed desde `conteoInicial`
- **`encomiendasService.js`**: `getActivas()` agregado en F1 y luego **eliminado** (sin callers tras quitar la lista del dashboard del guardia)
- **`useDashboardGuardia.js`**: `encomiendasQuery` (via `getActivas()`, arregla bug PageResponse vs array) creado en F1 y luego **eliminado**
- **`GuardiaDashboardView.vue`**: usa `TarjetaEncomiendasPendientes`; lista pendientes con campos reales (`nombreDestinatario`, `unidadNumero`, `tipo`, `creadoEn`)
- **`useEncomiendas.js`**: invalida `["encomiendas", cid]` al registrar/entregar (con optimistic update + rollback en entregar)
- **`docs/solicitudes-backend/REQUERIMIENTOS_STATS_TIEMPO_REAL.md`**: creado — informe de requerimientos SSE para estadísticas en tiempo real (entregable al backend, fase F2/F3)
- **`sseParser.js`**: creado — parser SSE puro e incremental (frames, `data` multilínea, `:ping`, CRLF) para leer el stream con `fetch()` + `ReadableStream` (EventSource no soporta headers)
- **`dashboardStreamService.js`**: creado — cliente SSE singleton vía `fetch()` + `ReadableStream` con `Authorization: Bearer`, dedupe por condominio, reconexión con backoff (1s→30s), `streamVivo` ref compartido a nivel de módulo
- **`useMetricasTiempoReal.js`**: creado — registro `clave SSE → queryKey` (visitasActivas soloConteo / autorizacionesPendientes invalidate / encomiendasPendientes soloConteo), `refetchIntervalMetrica` computed reactivo (**sin polling con stream vivo**; **gracia 1 min sin polling y luego fallback a 2 min solo si el stream sigue caído** — no inunda de peticiones), refetch del snapshot al reconectar, **no resetea `metricas` al reconectar el MISMO condominio** (el backend envía `SNAPSHOT_INICIAL` como primer frame y repuebla al instante; solo se limpia al cambiar de condominio), invalida `["dashboardGuardia", cid]` al reconectar
- **`MainLayout.vue`**: stream SSE conectado app-wide con `watchEffect` cuando hay condominio + permiso `DASHBOARD_GUARDIA` o `DASHBOARD_ADMIN`; se aborta al cambiar condominio/cerrar sesión; también invoca `useMetricasTiempoReal()` app-wide para que la suscripción a eventos/estado quede registrada en cualquier rol (arregla el badge del admin, que nunca registraba la suscripción por no montar el dashboard del guardia)
- **`docs/solicitudes-backend/SOLICITUD_SNAPSHOT_INICIAL_SSE.md`**: creado — solicitud al backend para publicar un snapshot de métricas (todas las claves) al suscribirse al stream, evitando que las cards queden stale entre el fetch del snapshot y el primer evento de cambio. **Backend lo implementó y verificó** (informe de vuelta): `SNAPSHOT_INICIAL` como primer frame del stream
- **`TarjetaAccesosActivos.vue`**: conteo en vivo solo vía SSE (`metricas.visitasActivas`); antes del primer evento muestra `conteoInicial` (sembrado desde `dashboard.accesos.activosAhora`) — ya NO consulta `GET /accesos/conteo-activos` (endpoint eliminado del service). Mismas condiciones que la tarjeta de encomiendas
- **`TarjetaEncomiendasPendientes.vue`**: conteo en vivo solo vía SSE (`metricas.encomiendasPendientes`); antes del primer evento muestra `conteoInicial` (sembrado desde `dashboard.encomiendas`) — ya NO consulta `GET /encomiendas/activas` (eliminada la lista completa del dashboard del guardia)
- **`GuardiaDashboardView.vue`**: eliminada la sección "Encomiendas pendientes" (lista) y su query `encomiendas/activas`; quedan visitas, unidades, residentes y autorizaciones; agregado indicador visual de conexión SSE (`estaVivo` de `useMetricasTiempoReal`) sobre la grilla de tarjetas
- **`AdminDashboardView.vue`**: el badge de accesos también recibe `:conteo-inicial` desde `dashboard.accesos.activosAhora` (seed del snapshot); NO usa composables de métricas — el conteo en vivo del badge llega porque `TarjetaAccesosActivos` importa el reactive `metricas` y la suscripción app-wide en `MainLayout.vue` lo puebla
- **`useDashboardGuardia.js`**: polling reactivo vía `refetchIntervalMetrica()` (sin polling con stream vivo; gracia 1 min y fallback a 2 min si el stream sigue caído); eliminado `encomiendasQuery` y `conteoActivosQuery`; `dashboard = computed(() => dashboardQuery.data)` (sin fallback `?? []` — el `[]` truthy enmascaraba el snapshot y renderizaba `conteo-inicial` 0)
- **`visitasService.js`**: eliminado `getConteoActivos` (sin callers tras mover la tarjeta de visitas a SSE puro)
- **`vite.config.js`**: `devOptions.enabled: false` para el PWA en dev — el SW (solo push, no cachea) se reinstalaba constantemente con `autoUpdate`; en producción el build lo incluye igual
- Agregados `console.error` en catch blocks de todas las vistas
- Corregido bug de loading infinito en `cargarUnidades()` (resuelve spinner cuando `!cid`)
- **Sistema de respaldo probado**: `useMetricasTiempoReal` con gracia de 1 min sin polling y fallback a 2 min solo si el stream sigue caído; cubierto por tests (`useMetricasTiempoReal.test.js`) y verificado en vivo contra el backend real (snapshot + eventos de cambio + `:ping`)
- **`DashboardView.vue` renombrado a `ResidenteDashboardView.vue`** (`src/views/dashboard/`): identificado claramente como el dashboard del residente (mismo patrón de nombre que `GuardiaDashboardView`/`AdminDashboardView`); actualizada la ruta `MiUnidad` en el router. La ruta `MiUnidad` y la nav "Mi unidad" se mantienen intactas
- **`InicioView.vue`**: la Card de encomiendas del residente (lista de pendientes desde `GET /mis-encomiendas`) se extrajo al componente reutilizable `TarjetaEncomiendasResidente`; el `Promise.all` ya no llama `getMisEncomiendas`
- **Docs reorganizados**: todos los `.md` de raíz se movieron a `docs/` por categoría — solicitudes/requerimientos al backend y verificaciones SSE en `docs/solicitudes-backend/`, QA en `docs/qa/`, especificación en `docs/especificacion/`, guías/integraciones en `docs/guias/`. Creada la solicitud pendiente `docs/solicitudes-backend/SOLICITUD_STREAM_ENCOMIENDAS_RESIDENTE.md`
- Build pasa con `pnpm build`

## Blocked
- **SolicitudesView**: no existe `SolicitudesController` en el backend — llama a `/condominios/{cid}/solicitudes-registro` que devuelve 404. **Mitigado temporalmente**: la ruta `Solicitudes` ahora renderiza `EnConstruccionView` (placeholder) hasta que el backend implemente el controller; el archivo `SolicitudesView.vue` se conserva en el repo para reconectarlo después.
- **PermisosMatrixView y CargosPermisosView (permisos por rol/cargo)**: no existen endpoints de administración de permisos en el backend (solo lectura efectiva vía `GET /me/permisos`). **Mitigado temporalmente**: las rutas `PermisosMatrix` y `CargosPermisos` renderizan `EnConstruccionView` (precedente `SolicitudesView`); los archivos `.vue` (100% mock, códigos inexistentes) se conservan para reconectarlos contra los endpoints de `SOLICITUD_ADMIN_PERMISOS_POR_CARGO.md` (`GET /admin/permisos/catalogo`, `GET/PUT /admin/cargos/{cargo}/permisos`, `GET/PUT /admin/roles/{rol}/permisos`).

## Componentes Creados
- **`PlanillaDatos.vue`** (`src/components/planilla/`): Componente reutilizable de planilla persona-por-fila. Agrupa por casa (colapsable, responsable primero), filtro por casa, botón "+" para nueva fila, celdas editables (InputText/Select/Checkbox), errores por fila, mobile cards + desktop fila tipo Excel, columnas de bodegas si `capacidadBodegas>0`. Usado por `ImportacionMasivaView.vue` y `SetupPlanillaView.vue`.
- **`InfoAyudaVista.vue`** (`src/components/common/`): Botón icono (i) + `Popover` de ayuda para vistas. Props: `titulo`, `secciones` (array `{titulo, items: [{label, desc}]}`). Usado en `SaasReglasNotificacionView.vue` y `ReglasNotificacionView.vue` para explicar cada opción de Audiencia/Canales/Prioridad (desde `AUDIENCIA_DESC`/`CANAL_DESC`/`PRIORIDAD_DESC` en `reglasCatalogo.js`, redactadas según los enums reales del backend `TipoAudiencia`/`CanalEntrega`/`PrioridadNotificacion`). Build OK + tests 82/82
- **`FiltrosBitacora.vue`** (`src/components/bitacora/`): Componente reutilizable de filtros colapsables de la Bitácora. Usa `defineModel("tipo"|"clasificacion"|"rangoFechas")` para los tres filtros (Select tipo, Select clasificación, FiltroFechas). Header "Filtros" + "Limpiar". Visible detrás de la lupa en `BitacoraView`.
- **`BuscadorPatenteCard.vue`** (`src/components/visitas/`): Componente reutilizable para búsqueda por patente. Props: `compact`. Consulta en paralelo `/busqueda/por-patente` + `GET /accesos?estado=ACTIVO` para detectar si hay acceso activo que permita salida rápida.
- **`ConfirmarSalidaDialog.vue`** (`src/components/visitas/`): Subcomponente del BuscadorPatenteCard. Dialog modal que muestra datos del acceso activo (visitante, unidad, fecha ingreso, tipo, personas) y permite confirmar salida con observación opcional.
- **`TarjetaEncomiendasPendientes.vue`**: Card reutilizable para conteo de encomiendas pendientes. Props: `variant` ('card'|'badge'), `conteoInicial` (seed del snapshot). Emite `click`. Conteo en vivo solo vía SSE (`metricas.encomiendasPendientes`) — no fetchea la lista completa.
- **`TarjetaAccesosActivos.vue`**: Card reutilizable para conteo de visitas activas. Props: `variant` ('card'|'badge'), `conteoInicial` (seed del snapshot). Emite `click`. Conteo en vivo solo vía SSE (`metricas.visitasActivas`) — no fetchea `GET /accesos/conteo-activos` (eliminado).
- **`TarjetaEncomiendasResidente.vue`** (`src/components/encomiendas/`): Card reutilizable que muestra la lista de encomiendas **pendientes** del residente (sus unidades). Props: `condominioId`, `limite` (default 5). Fetchea `GET /condominios/{cid}/mis-encomiendas` y filtra `estado === "PENDIENTE"`; muestra badge de conteo, navega a `MisEncomiendas` al hacer clic. Uso distinto al del guardia (que es conteo SSE vía `TarjetaEncomiendasPendientes`).
- **`AccesoRapidoCard.vue`** (`src/components/quickaccess/`): Card "Acceso rápido" extraída del dashboard del guardia a componente reutilizable. Props: `items` (array `{label, icon, routeName, query?, severity?, variant?, isCentralFab?}`), `title`, `columns`. Items con `isCentralFab` se renderizan como botón primary (destacado).
- **`BottomNavigation.vue`** (`src/components/layout/`, reescrito): Bottom Navigation Bar global estilo app nativa, solo en PWA instalada standalone + touch. Botón central flotante (FAB) con ícono de casa y label "Home" que lleva al dashboard del rol (`GuardiaDashboard`/`Dashboard`/`Inicio`) vía `isCentralFab`. Solo visible con rol con items configurados (`GUARDIA`/`ADMINISTRADOR`/`RESIDENTE`). Respeto `env(safe-area-inset-bottom)`.
- **`EnConstruccionView.vue`** (`src/views/common/`): Pantalla placeholder "en construcción". Recibe `modulo` por prop o `route.query.modulo`. Usada por la ruta `Solicitudes` (sin backend aún); la ruta `Escanear` queda registrada para un futuro escáner QR.

## Composable
- **`usePlanillaDatos.js`**: estado de la planilla persona-por-fila del ADMINISTRADOR. Define `validarFila` (reglas del import + dedupe contra backend: unidades/emails/patentes existentes + patentes duplicadas en archivo), `marcarResponsable` (1 `es_responsable` por casa), **borrador en sessionStorage** (autoguardado con `watch` deep, `cargarBorrador`/`descartarBorrador`, sobrevive recargas y pérdida de señal, se pierde al cerrar pestaña), `buildPayload` (todas las filas en una sola petición) y `enviar` simulado. Usado por `PlanillaDatos.vue` (manual + wizard + importación). Test `usePlanillaDatos.test.js` — 103/103 OK, build OK
- **`useSetupConfiguracion.js`**: estado del wizard de configuración del ADMINISTRADOR. Define `SETUP_PASOS` (5 pasos: `planilla` real + `accesos`/`areas-comunes`/`cargos`/`personal` como placeholders), deriva el progreso **desde datos reales** (dashboard `totales.residentesActivos > 0` para el paso planilla) sin tocar backend. Expone `cargar()` (fetch `dashboardService.admin`) y `sincronizarTotales(totales)` (para el banner del dashboard sin refetch). Usado por `SetupLayout.vue` (barra de progreso) y `AdminDashboardView.vue` (banner). Test `useSetupConfiguracion.test.js` — 103/103 OK, build OK
- **`useValidacionChile.js`**: Estándar de validación/formateo de formularios chilenos (RUT, teléfono, email, nombre). Envuelve `src/utils/validadoresChile.js` y expone `errores` (ref reactiva), `validarNombre/Rut/Email/Telefono(campo, mensaje)` (RUT y teléfono opcionales — solo validan si vienen con valor), transforms `onRutInput/onRutBlur/onTelefonoInput/onTelefonoBlur` (reciben el valor y devuelven el formateado, para bindear `@input="form.rut = onRutInput(form.rut)"`), `focusPrimerError(orden)` (enfoca el primer campo con error) y normalizadores `normalizarRut/Telefono/Email` para el body. Usado por `SaasCrearCondominioView.vue` y `SaasCondominioDetailView.vue`. Test `useValidacionChile.test.js` — 81/81 OK, build OK
- **`useBusquedaPatente.js`**: Orquesta dos consultas paralelas en `consultar()`: (1) `busquedaService.porPatente()` y (2) `accesosService.listar(estado=ACTIVO)` — filtra localmente por patente para detectar `accesoSalida`. Expone `confirmarSalida(observacion)`. Maneja errores de campo del backend (`ErrorResponse.fields[]`).
- **`usePwaStandalone.js`**: Detecta si la app corre como PWA instalada en dispositivo táctil. `matchMedia('(display-mode: standalone)')` + `navigator.standalone` (iOS) + `(pointer: coarse)`. Escucha `change`, `pageshow`, `focus` y `appinstalled`. Expone `{ isStandalone, esTouch, mostrar }` con `mostrar = isStandalone && esTouch`.
- **`useBottomNav.js`**: Consume `BOTTOM_NAV_BY_ROLE` según `condominioActualRol`, filtra con `puedeAcceder()` (helper ahora expuesto por `useNavigation`). `activo(item)` compara `route.path` (cubre subrutas). Expone `{ items, visible, activo, go }`.

## Flujo de Ingreso Rápido (desde búsqueda)
El guardia busca una patente → si el vehículo está identificado, botón "Registrar ingreso" navega a `RegistrarVisitaView` con `patente`, `nombre` (desde `titulo`), `unidad` (parseada de `subtitulo/detalle`) y `autorizacionId` (si es PREAUTORIZACION). El guardia completa los datos faltantes y confirma.

## Flujo de Salida Rápida
El guardia busca una patente → si hay un acceso ACTIVO con esa patente, aparece botón "Registrar salida" → Dialog muestra datos del acceso → confirma → `PATCH /accesos/{id}/salida`.

## Key Decisions
- **Listas tabulares → patrón "planilla" responsive**: en desktop tabla tipo Excel y en mobile cards desplegables. Reusable vía `.planilla` en `src/theme/app.css` (fondo sólido `surface`, zebra con `color-mix`, hover, `th.sortable`, `td.truncado`) + guía `docs/guias/tabla-planilla-responsive.md`. **Regla: NO usar `surface-card`/`border-round`/`shadow-1`** — son clases del viejo preset PrimeVue que no existen en este proyecto y dejan todo transparente (bug de `SaasAuditoriaView`); usar siempre `bg-surface`/`bg-background`/`text-text-muted`/`border-border`
- Los diálogos (`Dialog`/`ConfirmDialog`) usan los tokens `overlay.modal.*` que resolvían a `surface.0` = `textPrincipal` (fondo oscuro) con texto `text.color` oscuro → ilegibles en light. Se sobrescriben `dialog.root` y `confirmdialog.root` con el patrón `{primary.surface}` + `{primary.textPrincipal}`, igual que popover/select/autocomplete
- Eliminar todos los datos mock porque confundían la depuración — ahora el error real se ve en la consola del navegador
- PortonView ahora usa `GET /busqueda/por-patente` en vez de `GET /vehiculos` + filtro local
- `useEncomiendas.entregar()` ahora recibe `nombreRetira` y `rutRetira` como parámetros obligatorios
- Eliminado `buscarFrecuente()` de `RegistrarVisitaView` porque el backend no expone `GET /accesos/frecuentes`
- Las categorías de tipo de acceso ahora coinciden exactamente con el enum `TipoAutorizacion` del backend
- `useNotificaciones` ahora obtiene `condominioId` desde `authStore` internamente
- `useBusquedaPatente` consulta en paralelo `/busqueda/por-patente` + `/accesos?estado=ACTIVO` para detectar acceso activo y permitir salida rápida
- `ConfirmarSalidaDialog` es subcomponente presentacional — toda la lógica vive en `useBusquedaPatente`
- El botón "Registrar ingreso" en `BuscadorPatenteCard` navega a `RegistrarVisitaView` con datos pre-rellenados, evitando lógica duplicada de ingreso
- `RegistrarVisitaView` usa `AutoComplete` de PrimeVue para buscar casa por número con sugerencias al escribir
- **Con stream SSE vivo NO hay polling** de métricas (el SSE es la fuente primaria); si el stream cae, hay una gracia de 1 min sin polling (le da tiempo a la reconexión con backoff) y solo si sigue caído se activa el polling de respaldo a 2 min. Reducción de tráfico: 0 requests de `conteo-activos`/`encomiendas/activas` en reposo
- **Al reconectar el SSE** se invalidan las queries registradas (refetch del snapshot) para reconciliar deltas perdidos sin depender del polling
- **Eliminada la sección "Encomiendas pendientes"** del dashboard del guardia y con ella la petición `GET /encomiendas/activas` (lista completa). El conteo de la tarjeta sale del SSE (`metricas.encomiendasPendientes`) y antes del primer evento se siembra desde `dashboard.encomiendas` (snapshot ya real, no hardcodeado)
- **Ambas tarjetas de métricas (visitas y encomiendas) trabajan 100% con SSE** — misma condición y misma fuente primaria. El seed del snapshot es la única lectura inicial: `dashboard.accesos.activosAhora` (visitas) y `dashboard.encomiendas` (encomiendas). El fallback `dashboardQuery.data ?? []` se eliminó porque el `[]` (truthy) enmascaraba el snapshot y hacía renderizar `conteo-inicial = 0` en recargas con stream caído
- **Bottom Navigation Bar es PWA-only**: se muestra solo cuando `display-mode: standalone` (o iOS `navigator.standalone`) + `pointer: coarse`. El FAB central es "Home" (ícono casa) y navega al dashboard del rol; los sets por rol se definen en `BOTTOM_NAV_BY_ROLE` con `conHomeCentral()` (2 items a cada lado del FAB). Reemplaza la card "Acceso rápido" en móvil instalado (que ahora vive en `AccesoRapidoCard.vue`). En navegador de escritorio se mantiene la card del dashboard y el footer.
- **El stream del residente es un bus SSE independiente** del canal operativo: distinto endpoint (`/dashboard/residente/stream`), distinto permiso (`DASHBOARD_RESIDENTE`) y distinto payload (señales por unidad con `tipoEvento`, no `metricas[]` globales). Vive en `residenteStreamService.js` con su propio `streamResidenteVivo`; un usuario con permisos de ambos canales mantiene ambas conexiones. La invalidación de `["misEncomiendas", cid]` se dispara ante eventos de cambio y ante `SNAPSHOT_INICIAL` (reconciliación al conectar/reconectar, deltas perdidos durante la caída); el snapshot siembra el badge (`pendientesResidente`), que se descarta ante un cambio para no mostrar conteos stale
- **El stream de notificaciones es un tercer bus SSE independiente**: distinto endpoint (`/notificaciones/stream`), distinto permiso (`NOTIFICACION_VER`) y distinto payload (`{tipoEvento, condominioId, notificacionId, noLeidas}`). Vive en `notificacionesStreamService.js` con su propio `streamNotificacionesVivo`; un usuario con los tres permisos mantiene las tres conexiones. Invalida `["notificaciones-sync", cid]` (el snapshot también, para reconciliar); el snapshot siembra `noLeidas` (badge), que se descarta ante `NOTIFICACION_CREADA`. El intervalo de respaldo es reactivo en `useNotificaciones` (sin polling con stream vivo; gracia 1 min / fallback 2 min)
- **Deploy del frontend = Cloudflare Worker (no Coolify)**: el sitio se sirve en `comunidad.space-ideaserver.workers.dev` (Worker `comunidad`, estático desde `dist/` vía `wrangler.json`). CI/CD automático: al fusionar un PR a `main`, el GitHub App `cloudflare-workers-and-pages` construye y publica (check `Workers Builds: comunidad`, build en `dash.cloudflare.com/.../workers/services/view/comunidad/production`). `main` contiene `wrangler.json` que `develop` NO tenía — antes de un PR a `main`, mergear `main→develop` para no borrarlo del diff.

## Next Steps
- **Archivo maestro de acciones pendientes**: `docs/solicitudes-backend/ACCIONES_PENDIENTES.md` consolida todo lo pendiente (backend/QA/frontend) y el estado real de cada solicitud (varios archivos individuales quedaron desactualizados). Actualizarlo al completar/descubrir ítems
- **Auditoría de "desactivar plan" (backend)**: `AdminPlanesController.desactivar` (`PATCH /admin/planes/{id}/desactivar`) NO registra auditoría — falta `auditoriaService.planDesactivado(...)` (no existe el método ni el código `PLAN_DESACTIVAR`) y la llamada en el controller. Al implementarlo, agregar la acción `PLAN_DESACTIVAR` al filtro de `SaasAuditoriaView.vue` (hoy solo existe `PLAN_REACTIVAR`)
- **Importación masiva Excel/CSV (endpoint pendiente backend, contrato v2.0)**: solicitud `docs/solicitudes-backend/SOLICITUD_IMPORTACION_MASIVA_EXCEL_CSV.md` (nueva: formato persona-por-fila con `patente1..3`/`est1..3`/`bodega1..3`, campo `es_responsable`, batch JSON en `preview`/`ejecutar`). El frontend `ImportacionMasivaView.vue` y `SetupPlanillaView.vue` ya validan localmente el formato y "Importar datos"/"Guardar planilla" son simulados (pasan por `usePlanillaDatos.enviar`); al implementarse `POST /importaciones/preview` y `POST /importaciones/{id}/ejecutar`, conectar la vista (`importacionService` con `preview`/`ejecutar`/`plantilla`) y reemplazar la validación local por el contrato real (envío batch de `buildPayload`)
- **Pasos 2-5 del wizard (placeholders)**: `useSetupConfiguracion` define `accesos`/`areas-comunes`/`cargos`/`personal` como `EnConstruccionView`; `configuraciónCompleta` solo será true cuando esos pasos tengan lógica real. El CRM de datos (soft delete `PATCH .../desactivar`) es el paso natural para conectar una vista `GestionDatosView` con la misma `PlanillaDatos.vue`
- **Reenvío de email de configuración de contraseña (pendiente backend)**: pide (1) fix de `PasswordResetService.configurarPassword` sin `@Transactional` → **500 LazyInitializationException** en `setup-password` (el token válido se marca usado en `validarYConsumir` antes del 500, por eso queda quemado); y (2) endpoint `POST /condominios/{cid}/personas/{personaId}/usuario/reconfigurar` (permiso `USUARIO_GESTIONAR`) que genera un token `SETUP_PASSWORD` nuevo y reenvía el email. Uso de soporte: si el usuario X no pudo entrar → "Restablecer contraseña" (si ya configuró) o reenviar email de configuración (cuenta sin contraseña o link expirado/usado). El frontend lo conectará como acción "Reenviar email de configuración" en `SaasUsuariosView`/wizard cuando el backend lo implemente. Nota frontend: `authService.setupPassword/resetPassword` ya envían `passwordNueva`/`passwordNuevaConfirmacion` (el DTO `ResetPasswordRequest` los exige; antes enviaba `newPassword`/`confirmPassword` → 400 de validación)
- **Endpoint `GET /condominios/{cid}/capacidad-unidades` (✅ implementado V60)**: `CapacidadUnidadesController` expone `GET /api/v1/condominios/{condominioId}/capacidad-unidades` (`CapacidadUnidadesResponse`: capacidades + totales por tipo + `totalActual` + `planUnidadLimit`). El frontend ya lo consume (`unidadesService.getCapacidad` → `UnidadesView`, `SaasCondominioDetailView` con barra de envelope) con degradación suave. Verificado contra el detalle superadmin en la URL de revisión del condominio `47f29720-c745-4221-9928-453dad96149f`
- **Reproducir la pantalla blanca en el iPhone PWA con el build instrumentado** (desplegado, `index-CaiEwdUo.js`): al reanudar la app en blanco, leer en el teléfono `comunidad:html-loaded`, `comunidad:frontend-hitos` y `comunidad:frontend-errors` (o usar el overlay "Copiar error"); en paralelo, los beacons `/__frontend-boot` y `/__frontend-error` (404 intencional) quedan en los logs del Worker `comunidad` en Cloudflare (dashboard workers/services/view/comunidad/production → Logs). Distinguir: `html-loaded` sin `boot:main` = el bundle nunca cargó (SW/shell/red); `boot:main` con `frontend-errors` = crash de render (overlay muestra el detalle)
- **Stream de notificaciones verificado en vivo (dev)**: pendiente — `GET /condominios/{cid}/notificaciones/stream` con `NOTIFICACION_VER` responde `SNAPSHOT_INICIAL` (`noLeidas` real) + `:ping`; una notificación creada para la persona X llega SOLO a la conexión de X. Verificar aislamiento por persona con dos pestañas y el toggle `recibeNotificaciones=false` (no recibe señal)
- **Stream residente verificado en vivo (dev)**: ✅ `GET /condominios/{cid}/dashboard/residente/stream` responde `SNAPSHOT_INICIAL` (`pendientes` real) como primer frame + `:ping`; evento `ENCOMIENDA_RECIBIDA` llega al stream del residente al registrar una encomienda (E2E con guardia → residente, `pendientes` 3→4). La invalidation `["misEncomiendas", cid]` dispara el refetch. Pendiente verificar en staging/prod según checklist en `docs/solicitudes-backend/verificacion-sse-staging-prod.md`
- **403 de `/me/permisos` resuelto (backend)**: `PermisosCondominioService` ahora delega acceso en `CondominioAccessService.tieneAcceso` (usuario_condominios | miembro | vínculo) y cae al rol global + cargo si no hay fila contextual. Verificado en vivo: residente sin cargo 16 permisos (incl. `DASHBOARD_RESIDENTE`, sin `DASHBOARD_GUARDIA/ADMIN`), guardia 19, admin 71. Sin cambios en frontend (la navegación es por rol, no por permiso → sin sobre-exposición de UI)
- Verificar `SNAPSHOT_INICIAL` en dev: ✅ verificado en vivo (backend reiniciado) — primer frame `event: metrica` con `tipoEvento: SNAPSHOT_INICIAL` y las 3 claves (visitasActivas 7, encomiendasPendientes 7, autorizacionesPendientes 0) coincidiendo con `GET /dashboard/guardia`; evento de cambio `ENCOMIENDA_RECIBIDA` incrementa el conteo; `:ping` cada ~15s. Pendiente verificar en staging/prod según checklist en `docs/solicitudes-backend/verificacion-sse-staging-prod.md`
- Verificar que todas las vistas funcionan contra la API real
- Solicitar al backend: crear `SolicitudesController` para `/condominios/{cid}/solicitudes-registro`
- Solicitar al backend (mejora): crear endpoint `GET /condominios/{cid}/vehiculos/consulta-rapida?patente=X`
- Migrar vistas legacy que aún usan daisyUI (`GestionesView`, `EncomiendasView`, `SolicitudesView`) a PrimeVue## Critical Context
- Backend en `/home/jhonayo/dev/projects/portfolio/comunidad/` — NO modificar
- `TipoAutorizacion` enum: `VISITA`, `DELIVERY`, `UBER`, `SERVICIO`, `TECNICO`, `OTRO`
- `TipoEncomienda` enum: `CARTA`, `ENCOMIENDA`
- `EstadoAcceso` enum: `ACTIVO`, `FINALIZADO`, `RECHAZADO`
- `TipoEventoBitacora` enum: `TURNO_INICIO`, `TURNO_FIN`, `COLACION_SALIDA`, `COLACION_REGRESO`, `NOVEDAD`
- `ClasificacionBitacora` enum: `INFO`, `NORMAL`, `URGENTE`, `EMERGENCIA`
- `DashboardGuardiaResponse` record: `condominio`, `totalUnidades`, `residentesActivos`, `accesos` (con `activosAhora` + `ultimosMovimientos`), `encomiendas` (conteo PENDIENTE real, ya no hardcodeado a 0 — sirve de seed para la tarjeta)
- `Movimiento` record: `nombre`, `tipo`, `unidad`, `fecha`
- `ResidenteDashboardResponse` record: `nombre`, `email`, `unidades` (lista de `UnidadInfo`)
- `UnidadInfo` record: `id`, `numero`, `tipo`, `vehiculos`, `personas`, `gastoActual`
- `GastoUnidadInfo` record: `periodo`, `fechaVencimiento`, `monto`, `estadoPago`, `fechaPago`
- `EncomiendaResumenResponse`: `id`, `unidadNumero`, `tipo`, `nombreDestinatario`, `estado`, `creadoEn`, `creadoPorNombre`, `nombreRetira`, `rutRetira`, `observaciones`
- `NotificacionResponse`: `id`, `tipo`, `titulo`, `mensaje`, `tipoRecurso`, `recursoId`, `prioridad`, `leido`, `fechaCreacion`, `fechaLectura`
- `TipoAudiencia` enum (backend): `PERSONA`, `UNIDAD`, `UNIDAD_OCUPANTES` (esOcupante de la unidad), `UNIDAD_TITULAR` (PROPIETARIO/ARRENDATARIO, excluye adicionales), `COMITE`, `GUARDIAS`, `ADMINISTRADORES`, `PROPIETARIOS`, `RESIDENTES`, `TODOS`
- `NotificacionStreamEvent` (SSE bandeja): `{tipoEvento: SNAPSHOT_INICIAL|NOTIFICACION_CREADA, condominioId, notificacionId, noLeidas, timestamp}` — `noLeidas` solo en snapshot, `notificacionId` solo en cambio; la lista se lee por `GET /sync`
- `VinculoResponse`: `id`, `personaId`, `personaNombre`, `unidadId`, `unidadNumero`, `tipo`, `esOcupante`, `recibeNotificaciones`, `fechaInicio`, `fechaFin`, `activo`
- `MeResponse`: `personaId`, `nombre`, `email`, `roles`
- Build pasa con `pnpm build`
- `.env` apunta a `https://apicomunidad.ideaspace.dpdns.org/api/v1`

## Relevant Files
- `src/data/planillaColumnas.js`: fuente única del esquema persona-por-fila (`COLUMNAS_BASE`, `columnasVehiculos`, `columnasBodegas`, `generarColumnas(capacidad)`, `COLUMNAS_DEFAULT`, `filaAPayload`, enums `TIPOS_UNIDAD/VINCULO/VEHICULO`). Ver "Modelo de planilla persona-por-fila" en Done
- `src/composables/usePlanillaDatos.js` + `src/composables/__tests__/usePlanillaDatos.test.js`: estado de filas, `validarFila` (reglas del import + dedupe backend), `marcarResponsable`, borrador sessionStorage, `buildPayload` batch, `enviar` simulado
- `src/components/planilla/PlanillaDatos.vue`: componente reutilizable de planilla (filtro por casa, grupos colapsables, responsable primero, errores por fila, mobile cards + desktop fila Excel, columnas bodegas si `capacidadBodegas>0`)
- `src/utils/csvParser.js` + `src/utils/__tests__/csvParser.test.js` + `src/views/admin/ImportacionMasivaView.vue` (ruta `importacion`, nav admin): simulación de importación masiva Excel/CSV (parser puro + validación local + `PlanillaDatos` + botón "Importar datos" simulado). Ver "Modelo de planilla persona-por-fila" en Done
- `src/views/setup/SetupLayout.vue` + `src/views/setup/SetupPlanillaView.vue` + `src/composables/useSetupConfiguracion.js`: wizard de configuración del administrador (ruta base `setup`, paso 1 planilla real + pasos 2-5 `accesos`/`areas-comunes`/`cargos`/`personal` como placeholders `EnConstruccionView`, progreso derivado de datos). Ver "Modelo de planilla persona-por-fila" en Done
- `src/views/dashboard/AdminDashboardView.vue`: banner "Configuración pendiente" derivado de `dashboard.totales` vía `sincronizarTotales`
- `src/theme/app.css`: tokens Tailwind (`bg-surface`, `bg-background`, `text-text`, `border-border`, ...) + componente `.planilla` (tabla Excel responsive: fondo sólido `surface`, zebra `color-mix`, `th.sortable`, `td.truncado`); guía `docs/guias/tabla-planilla-responsive.md`
- `src/views/superadmin/SaasAuditoriaView.vue`: ejemplo del patrón planilla (filtros con AutoComplete de condominios + Select de acción/orden + `FiltroFechas`, tabla `.planilla` desktop + cards desplegables `md:hidden`); consume `GET /admin/auditoria?sort=createdAt,{asc|desc}` (default desc, ver `docs/solicitudes-backend/SOLICITUD_ORDENAMIENTO_AUDITORIA.md`)
- `src/config/navegacionAccesoRapido.js`: fuente única de ítems de acceso rápido/bottom nav por rol (`ACCESO_RAPIDO_GUARDIA` + `BOTTOM_NAV_BY_ROLE` con GUARDIA/ADMINISTRADOR/RESIDENTE, FAB central "Home" vía `conHomeCentral()`)
- `src/composables/usePwaStandalone.js`: detección PWA standalone + touch (ver Composable)
- `src/composables/useBottomNav.js`: ítems + estado activo + navegación de la bottom nav (ver Composable)
- `src/components/layout/BottomNavigation.vue`: barra global con FAB central (ver Componentes Creados)
- `src/components/quickaccess/AccesoRapidoCard.vue`: card reutilizable de accesos rápidos (ver Componentes Creados)
- `src/services/encomiendasService.js`: `getEncomiendas`, `getEncomienda`, `registrar`, `entregar` (PATCH `.../{id}/entregar`), `cerrar` (PATCH `.../{id}/cerrar`), `getMisEncomiendas` (URL corregida `/condominios/{cid}/mis-encomiendas`), `getAccesosEncomiendas` (`/encomiendas/accesos`); **`getActivas` eliminado**
- `src/services/perfilService.js`: Added `actualizarMe()`
- `src/composables/useNotificaciones.js`: Fixed method calls, added condominioId; usa `getTodas`, `getSync` (polling 120s), `marcarLeida`, `marcarTodasLeidas`
- `src/services/notificacionesService.js`: `getTodas`, `getBadge`, `getSync`, `marcarLeida` (PATCH `.../{id}/leida`), `marcarTodasLeidas` (PATCH `.../todas-leidas`), `listarPlantillas`, `guardarPlantilla`, `restaurarPlantilla`
- `src/views/notificaciones/NotificacionesView.vue`: PrimeVue migration, field fixes
- `src/views/encomiendas/MisEncomiendasView.vue`: PrimeVue migration, field fixes
- `src/views/residente/PerfilView.vue`: PrimeVue migration, real API
- `src/views/residente/InicioView.vue`: Added console.error
- `src/components/stats/TarjetaEncomiendasPendientes.vue`: Reusable pending-packages count card — conteo solo vía SSE (`metricas.encomiendasPendientes`), seed desde `conteoInicial`, sin `GET /encomiendas/activas`
- `src/components/stats/TarjetaAccesosActivos.vue`: Reusable active-visits count card — conteo solo vía SSE (`metricas.visitasActivas`), seed desde `conteoInicial`, sin `GET /accesos/conteo-activos`
- `src/composables/useDashboardGuardia.js`: `encomiendasQuery` y `conteoActivosQuery` eliminados; `dashboardQuery` y `autorizacionesQuery` con `refetchIntervalMetrica` (sin polling con stream vivo; gracia 1 min / fallback 2 min); `dashboard = computed(() => dashboardQuery.data)` (sin fallback `?? []`)
- `src/services/visitasService.js`: `getConteoActivos` eliminado (sin callers)
- `src/views/dashboard/GuardiaDashboardView.vue`: indicador visual de conexión SSE (`estaVivo`) + `conteo-inicial` en tarjeta de accesos
- `src/utils/sseParser.js`: Pure incremental SSE frame parser (frames, multiline data, `:ping`, CRLF) — tested with Vitest
- `src/composables/__tests__/useMetricasTiempoReal.test.js`: prueba el sistema de respaldo con módulos reales + timers falsos — stream vivo sin polling, gracia de 1 min sin polling, fallback a 2 min tras la gracia, vuelve a SSE al reconectar, parpadeo breve no activa polling
- `src/services/dashboardStreamService.js`: SSE client via `fetch()` + `ReadableStream`, reconnect backoff, dedupe by condominio, `streamVivo` singleton ref
- `src/services/residenteStreamService.js`: cliente SSE scoped al residente (canal "mis encomiendas", `dashboard/residente/stream`, permiso `DASHBOARD_RESIDENTE`) — bus INDEPENDIENTE del canal operativo; mismo transporte, `streamResidenteVivo` ref de módulo
- `src/composables/useMetricasTiempoReal.js`: clave→queryKey registry + `refetchIntervalMetrica` (sin polling con stream vivo; gracia 1 min / fallback 2 min)
- `src/composables/useMisEncomiendasTiempoReal.js`: invalida `["misEncomiendas", cid]` ante `ENCOMIENDA_RECIBIDA/ENTREGADA/CERRADA` y `SNAPSHOT_INICIAL`; seed del badge desde `pendientes` del snapshot; `refetchIntervalResidente` (gracia 1 min / fallback 2 min)
- **`src/services/residenteStreamService.js`**: creado — cliente SSE scoped al residente (canal "mis encomiendas", `GET /condominios/{cid}/dashboard/residente/stream`, permiso `DASHBOARD_RESIDENTE`). Bus INDEPENDIENTE del canal operativo (distinto endpoint/payload/permiso); mismo transporte (fetch + ReadableStream, backoff 1s→30s, dedupe por condominio, `streamResidenteVivo` ref de módulo)
- **`src/composables/useMisEncomiendasTiempoReal.js`**: creado — invalida `["misEncomiendas", cid]` ante `ENCOMIENDA_RECIBIDA/ENTREGADA/CERRADA` y `SNAPSHOT_INICIAL` (reconciliación al conectar/reconectar); siembra el badge desde `pendientes` del snapshot y lo descarta ante un cambio; `refetchIntervalResidente` con la misma gracia 1 min / fallback 2 min (contrato en `docs/sse-residente-mis-encomiendas.md` del backend)
- **`src/layouts/MainLayout.vue`**: el `watchEffect` también conecta el stream residente cuando el permiso es `DASHBOARD_RESIDENTE` (canal independiente; un usuario con ambos permisos mantiene ambas conexiones); `onUnmounted` detiene ambos
- **`src/services/notificacionesStreamService.js`**: creado — cliente SSE scoped por persona (canal "bandeja", `GET /condominios/{cid}/notificaciones/stream`, permiso `NOTIFICACION_VER`, evento `notificacion`). Bus INDEPENDIENTE de dashboard/residente (distinto endpoint/payload/permiso); mismo transporte (fetch + ReadableStream, backoff 1s→30s, dedupe por condominio, `streamNotificacionesVivo` ref de módulo)
- **`src/composables/useNotificacionesTiempoReal.js`**: creado — invalida `["notificaciones-sync", cid]` ante `NOTIFICACION_CREADA` y `SNAPSHOT_INICIAL` (reconciliación al conectar/reconectar); siembra el badge desde `noLeidas` del snapshot y lo descarta ante un cambio; `refetchIntervalNotificaciones` con la misma gracia 1 min / fallback 2 min (contrato en `docs/solicitudes-backend/SOLICITUD_MOTOR_NOTIFICACIONES_V2.md` del backend)
- **`src/views/gestion/UnidadesPersonasView.vue`**: creado — vista CRM "Unidades y Personas" (ruta `notificaciones/unidades-personas`, roles admin/super_admin): Select de unidad → vínculos con `tipo`, `esOcupante`, toggle `recibeNotificaciones` → `PATCH .../vincular-persona-unidad/{id}/recibe-notificaciones` (agregado a `personasService.actualizarRecibeNotificaciones`)
- **`ReglasNotificacionView.vue`**: ahora edita `audiencia` (Select con `UNIDAD_OCUPANTES`/`UNIDAD_TITULAR`) y `prioridad` (PUT `/personal/reglas-notificacion/{tipo}`) — antes solo mostradas
- **`PerfilView.vue`**: añadido toggle Push en preferencias (hoy App/Email/Push)
- **`useNotificaciones.js`**: `refetchInterval` del sync pasó de `120000` fijo a reactivo (`refetchIntervalNotificaciones` — sin polling con stream vivo; gracia 1 min / fallback 2 min); `noLeidasCount` se siembra desde `noLeidasNotificaciones` (snapshot SSE) con fallback a `syncData.noLeidas`
- **`useNotificacionesTiempoReal.test.js`**: cubre invalidación por `NOTIFICACION_CREADA`, seed del snapshot + descarte, y el sistema de respaldo (gracia/fallback/reconexión). Tests 27/27 OK, build OK
- **`TarjetaEncomiendasResidente.vue`**: migrado a `useQuery(["misEncomiendas", cid])` con `refetchInterval` reactivo (sin polling con stream vivo; fallback 2 min); badge sembrado desde el snapshot SSE (`pendientesResidente`) con fallback a longitud de lista
- **`useMisEncomiendasTiempoReal.test.js`**: cubre invalidación por evento de cambio, seed del snapshot + descarte, y el sistema de respaldo (gracia/fallback/reconexión). Tests 24/24 OK, build OK
- `src/layouts/MainLayout.vue`: app-wide SSE mount via `watchEffect` (condominio + permiso `DASHBOARD_*`)
- `docs/solicitudes-backend/REQUERIMIENTOS_STATS_TIEMPO_REAL.md`: SSE requirements report for the backend team
- `docs/solicitudes-backend/SOLICITUD_STREAM_ENCOMIENDAS_RESIDENTE.md`: solicitud del stream SSE de encomiendas scoped al residente (`dashboard/residente/stream`, permiso `DASHBOARD_RESIDENTE`) — **implementada por el backend** (contrato en `docs/sse-residente-mis-encomiendas.md` del backend) e integrada en el frontend
- **`docs/solicitudes-backend/SOLICITUD_MOTOR_NOTIFICACIONES_V2.md`**: creado — solicitud backend con 6 puntos: (1) fix `procesarEvento` para usar `catalogo.obtener(tipo, condominioId)` + manejo de regla null (hoy las sobrescrituras por condominio no se aplican al envío real y `habilitada=false` da NPE); (2) audiencias granulares `UNIDAD_OCUPANTES` (esOcupante de la unidad) y `UNIDAD_TITULAR` (PROPIETARIO/ARRENDATARIO, excluye adicionales) + queries; (3) `PATCH .../vincular-persona-unidad/{id}/recibe-notificaciones` (toggle del CRM); (4) stream SSE `GET /condominios/{cid}/notificaciones/stream` (`NOTIFICACION_VER`, scoped por persona) con snapshot mínimo `{noLeidas}` + señal `NOTIFICACION_CREADA` (lista por REST `/sync`, nunca por SSE); (5) alinear `ResidenteEncomiendasEventHandler` con `esOcupante=true`; (6) deprecar `/accesos/conteo-activos` y `/encomiendas/activas`, mantener vivo `/badge` (PushManager lo usa como backup). **Implementada por el backend** (commits `d15b3d7`+`0d22b7d`, suite 287 tests) e integrada en el frontend (ver Done)
- `docs/solicitudes-backend/verificacion-sse-staging-prod.md`: Pending SSE verification checklist for staging/prod
- Backend controllers: `AccesoController`, `AutorizacionController`, `BitacoraController`, `EncomiendaController`, `MisEncomiendaController`, `UnidadController`, `VehiculoController`, `PersonaController`, `DashboardController`, `DashboardStreamController`, `ResidenteDashboardController`, `NotificacionController`, `MeController`
